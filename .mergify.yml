pull_request_rules:
  - name: Merge pull requests from specific users
    conditions:
      - or:
          - files=package.json
          - files=package-lock.json
    actions:
      merge:
        method: merge
